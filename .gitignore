# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.env
.env.*
!.env.example
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Deploy artifacts. The app deploys to Vercel; Nitro picks its preset from the
# host's own env vars, so no platform config is committed. Security headers are
# applied in src/lib/security-headers.ts instead, which works on any host.
.vercel
.netlify
deploy-netlify
